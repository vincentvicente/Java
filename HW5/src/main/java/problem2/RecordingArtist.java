package problem2;

/**
 * Class representing a recording artist.
 */
public class RecordingArtist extends Person {
  /**
   * Constructs a RecordingArtist with the specified first and last name.
   *
   * @param firstName The first name of the recording artist.
   * @param lastName The last name of the recording artist.
   */
  public RecordingArtist(String firstName, String lastName) {
    super(firstName, lastName);
  }
}
